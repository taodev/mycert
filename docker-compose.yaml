version: '3.9'

services:
  mycert-service:
    build: .
    hostname: mydev
    environment:
      - ADDR=0.0.0.0:8443
      - CERT_DIR=/mycert/certs
      - CAROOT=/mycert/ca
    ports:
      - "8443:8443"
